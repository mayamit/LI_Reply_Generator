# Copy to .env and adjust as needed (local-only, never commit .env)

# API
API_HOST=127.0.0.1
API_PORT=8000

# Database (SQLite path relative to project root)
DATABASE_URL=sqlite:///data/app.db

# LLM provider (set at most one; Anthropic preferred if both present)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# LLM_TIMEOUT_SECONDS=30
